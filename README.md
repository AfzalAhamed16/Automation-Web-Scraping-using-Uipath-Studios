This project demonstrates how UiPath RPA can be effectively used to automate real-world data extraction tasks from web applications and convert them into meaningful business data stored in Excel.

Objective
Automate data extraction from a web table in Google Chrome
Convert unstructured web data into a structured Excel file
